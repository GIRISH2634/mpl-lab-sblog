specVersion: "v1beta"
serviceId: "blogapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "blogapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
